name: mips_linux_gcc

on: [push, pull_request]

jobs:

  mips_linux_gcc:
    runs-on: ubuntu-18.04
    steps:
    - uses: actions/checkout@v2
    - run: sudo apt-get -y -qq update
    - run: sudo apt install -y qemu-user
    - name: Prepare Dockcross
      run: |
        git clone --depth 1 https://github.com/dockcross/dockcross.git && cd dockcross
        docker run --rm dockcross/linux-mips > ./dockcross-linux-mips
        chmod +x ./dockcross-linux-mips
    - name: Verify tools version
      run: ./dockcross/dockcross-linux-mips bash -c '$CC --version'
    - name: make
      run: ./dockcross/dockcross-linux-mips make LDFLAGS=-static
    - run: file ./hello
    - run: qemu-mips ./hello
