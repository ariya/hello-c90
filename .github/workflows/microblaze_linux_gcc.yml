name: microblaze_linux_gcc

on: [push, pull_request]

jobs:

  microblaze_linux_gcc:
    runs-on: ubuntu-20.04
    timeout-minutes: 5
    env:
      CC: ./microblaze-linux-musl-cross/bin/microblaze-linux-musl-gcc
      LDFLAGS: -static
    steps:
    - uses: actions/checkout@v3
    - run: sudo apt-get -y -qq update
    - run: sudo apt install -y qemu-user
    - name: Prepare cross-compiler
      run: |
        curl -O https://musl.cc/microblaze-linux-musl-cross.tgz
        tar xzf microblaze-linux-musl-cross.tgz
    - name: Verify tools version
      run: ./microblaze-linux-musl-cross/bin/microblaze-linux-musl-gcc --version
    - run: make
    - run: file ./hello
    - run: qemu-microblaze ./hello
